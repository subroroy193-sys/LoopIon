import 'package:flutter/material.dart';

class TelematicsHeatmapScreen extends StatelessWidget {
  const TelematicsHeatmapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Telematics Heatmap'),
      ),
      body: const Center(
        child: Text('Telematics Heatmap Screen'),
      ),
    );
  }
}
