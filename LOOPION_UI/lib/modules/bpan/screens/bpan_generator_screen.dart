import 'package:flutter/material.dart';

class BpanGeneratorScreen extends StatelessWidget {
  const BpanGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BPAN Generator'),
      ),
      body: const Center(
        child: Text('BPAN Generator Screen'),
      ),
    );
  }
}
