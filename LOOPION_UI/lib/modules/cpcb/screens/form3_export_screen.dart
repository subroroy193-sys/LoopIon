import 'package:flutter/material.dart';

class Form3ExportScreen extends StatelessWidget {
  const Form3ExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Form 3 Export'),
      ),
      body: const Center(
        child: Text('Form 3 Export Screen'),
      ),
    );
  }
}
